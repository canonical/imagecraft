---
ignore-from-file: [.gitignore, .yamlignore]

extends: default

rules:
  document-start: disable
  float-values: enable
  line-length: disable
  octal-values: enable
  truthy:
    check-keys: false
