name: TICS
on:
  push:
    branches:
      - "main"
      - "tiobe"

jobs:
  TICS:
    uses: canonical/starflow/.github/workflows/tics.yaml@main
    with:
      project: "imagecraft"
    secrets: inherit
