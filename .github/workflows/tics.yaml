name: TICS
on:
  push:
    branches:
      - "main"
      # For troubleshooting
      - "work/tiobe**"

jobs:
  TICS:
    permissions:
      contents: read
    uses: canonical/starflow/.github/workflows/tics.yaml@main
    with:
      project: "imagecraft"
    secrets:
      TICSAUTHTOKEN: ${{ secrets.TICSAUTHTOKEN }}
